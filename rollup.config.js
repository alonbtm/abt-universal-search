/**
 * Root Rollup Configuration
 * This configuration handles builds for all packages in the monorepo
 */

export default [
  // Core package build (main library)
  './packages/core/rollup.config.js'
  // Additional package configs can be added here as the project grows
];